# HumTrack AI — Python Dependencies
# Optimised for RTX 4060 (8GB VRAM) on Windows/Mac/Linux
#
# Install all:
#   pip install -r requirements.txt
#
# For CUDA 12.1 (recommended for RTX 4060):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   then: pip install -r requirements.txt
#
# For CPU-only (no GPU):
#   pip install torch torchvision torchaudio
#   then: pip install -r requirements.txt

# ── Audio Recording & Signal Processing ──────────────────────────────────────
sounddevice==0.4.7
numpy==1.26.4
scipy==1.13.1

# ── PyTorch (install manually first — see header) ────────────────────────────
# torch>=2.2.0          # install via pytorch.org for correct CUDA version
# torchvision>=0.17.0
# torchaudio>=2.2.0

# ── Hugging Face — Model Loading ─────────────────────────────────────────────
transformers==4.44.2
accelerate==0.33.0
huggingface_hub==0.24.6
tokenizers==0.19.1
safetensors==0.4.4
sentencepiece==0.2.0

# ── 4-bit Quantization (fits models in 8GB VRAM) ─────────────────────────────
bitsandbytes==0.43.3

# ── ABC Notation → MIDI Conversion ───────────────────────────────────────────
symusic==0.4.5

# ── MIDI Output → FL Studio ──────────────────────────────────────────────────
mido==1.3.2
python-rtmidi==1.5.8

# ── MusicGen Audio Preview (optional Stage 3) ────────────────────────────────
audiocraft==1.3.0
encodec==0.1.1
xformers==0.0.27           # speeds up MusicGen attention on RTX 4060

# ── Terminal UI ──────────────────────────────────────────────────────────────
rich==13.7.1

# ── Utilities ────────────────────────────────────────────────────────────────
requests==2.32.3
tqdm==4.66.4
packaging==24.1
filelock==3.15.4
regex==2024.7.24