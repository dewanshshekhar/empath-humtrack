# HumTrack AI — Python Dependencies
# Optimised for RTX 4060 (8GB VRAM) on Windows/Mac/Linux
#
# Install PyTorch FIRST (see below), then run:
#   uv pip install -r requirements.txt
#
# PyTorch for CUDA 12.1 (RTX 4060):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121 --no-cache-dir

# ── Audio Recording & Signal Processing ──────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.26.0
scipy>=1.13.0

# ── Hugging Face — Model Loading ─────────────────────────────────────────────
transformers>=4.44.0
accelerate>=0.33.0
huggingface_hub>=0.24.0
tokenizers>=0.19.0
safetensors>=0.4.0
sentencepiece>=0.2.0

# ── 4-bit Quantization (fits models in 8GB VRAM) ─────────────────────────────
bitsandbytes>=0.43.0

# ── ABC Notation → MIDI Conversion ───────────────────────────────────────────
symusic>=0.4.5

# ── MIDI Output → FL Studio ──────────────────────────────────────────────────
mido>=1.3.0
python-rtmidi>=1.5.0

# ── MusicGen Audio Preview (optional Stage 3) ────────────────────────────────
audiocraft>=1.3.0
encodec>=0.1.1
xformers>=0.0.22

# ── Terminal UI ──────────────────────────────────────────────────────────────
rich>=13.7.0

# ── Utilities ────────────────────────────────────────────────────────────────
requests>=2.32.0
tqdm>=4.66.0
packaging>=23.1
filelock>=3.15.0
regex>=2024.7.0